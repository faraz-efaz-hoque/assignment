package game;

// NEW: enum for permanent abilities. Items can hold them; Actor.hasAbility()
// checks every item in the carrier's inventory, so no extra wiring needed.
public enum Abilities {
    STERILISING
}
